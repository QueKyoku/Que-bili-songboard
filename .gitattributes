# 换行符规范化
#
# 为什么需要：Windows 上 git 默认 core.autocrlf 会把 LF 转成 CRLF。
# 对 .bat 来说这是**必须**的（Windows 批处理用 LF 换行会执行出错），
# 但对 Python / shell 脚本又希望统一成 LF。
# 这里显式定死，避免不同人 clone 下来行为不一致。

# 默认：文本文件在仓库里统一用 LF
* text=auto eol=lf

# Windows 批处理必须 CRLF，否则 cmd 解析会出问题
*.bat text eol=crlf
*.cmd text eol=crlf

# PowerShell 脚本用 CRLF 更稳（Windows PowerShell 5.1 对 LF 的 .ps1
# 在少数情况下会有解析问题，而且中文注释 + BOM 已经踩过坑）
*.ps1 text eol=crlf

# 二进制文件：不要做任何换行转换
*.dll binary
*.exe binary
*.obj binary
*.pdb binary
*.lib binary
*.exp binary
*.zip binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary

# 词法分析/统计用的排除（GitHub 语言统计）
*.md linguist-documentation
