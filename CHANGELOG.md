# 更新日志

本文件记录本项目所有值得写下来的变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> ⚠️ 本项目依赖 B 站弹幕长连接和网易云的**非官方接口**，
> 接口一改就可能失效。补丁版本里也可能会出现"适应上游变更"的修复。

## [0.4.2] - 2026-09-20

### 变更

- **扫码登录的文件都收进 `tools/`，根目录只留 `扫码登录.bat` 一个入口**：

  | 位置 | 是什么 |
  | --- | --- |
  | `扫码登录.bat`（根目录） | 唯一入口：检查环境 → 拉起窗口 |
  | `tools/扫码登录.pyw` | 图形界面本体 |
  | `tools/login_qrcode.py` | 命令行版（终端里画二维码） |

  > 为什么不能合并成一个文件：`.bat` 负责"检查环境 + 启动"，
  > 但**批处理画不了窗口**，所以图形界面必须有个 `.pyw` 本体；
  > 而没装 Python 时 `.pyw` 双击又不会被执行，所以 `.bat` 这个入口也省不掉。
  > 两者是分工，但可以让**根目录只暴露一个**。

- 跟着同步了所有引用：`扫码登录.bat` 的启动路径、`tools/build_gui.ps1` 的打包目标、
  `tools/check_gui.py` 的加载路径，以及各处注释和 README。
  改完重新验证过：`扫码登录.bat` 能拉起窗口、`tools/build_gui.ps1` 打出的 exe 能运行、
  `tools/check_gui.py` 全过。
- 顺手清掉了本地的构建产物（`dist/`、`build/`、`*.spec`、根目录那个 23 MB 的 exe）——
  它们本来就在 `.gitignore` 里，只是留在文件夹里碍眼。要 exe 随时跑一次
  `tools/build_gui.ps1` 就有。
- `CHANGELOG.md` 里的历史记录**有意不改**：那几条记的是当时的事实，
  那时文件确实叫 `扫码登录.pyw` 且放在根目录。

## [0.4.1] - 2026-09-20

### 修复

- **补上「用户电脑上没装 Python」这个前置检测。**
  上一版把扫码登录做成 `扫码登录.pyw` 双击即用，但漏了一件事：
  **没装 Python 的时候，`.pyw` 双击根本不会被执行** ——
  Windows 只会弹一句「你要用什么打开这个文件？」，里面的代码一行都跑不到。
  也就是说"检查环境"这件事**不可能写在 `.pyw` 内部**。

  所以新增 **`扫码登录.bat`** 作为入口（`.bat` 不需要任何前置条件就能跑），
  它会把该检查的都检查一遍：

  | 情况 | 处理 |
  | --- | --- |
  | 没装 Python | 告诉你怎么装（含「务必勾选 Add python.exe to PATH」），并给出备选：用打包好的 exe |
  | Python 低于 3.10 | 报出当前版本，让你升级 |
  | 没有 `tkinter` | 提示换官方安装包，或改用命令行版 `python login_qrcode.py` |
  | 缺 `qrcode` | **自动 `pip install qrcode`**，装不上给手动命令 |
  | 一切正常 | 用 `pythonw` 拉起图形界面，不留黑色控制台窗口 |

  五条分支都用真实 cmd 实测过（正常 / 没 Python / 版本旧 / 没 tkinter /
  缺库且装不上），每条都给得出人话提示、都不会继续往下跑。

### 变更

- `启动.bat` 主菜单加了 **`[4] 扫码登录网易云`**，从那儿进也一样
  （内部转给 `扫码登录.bat`，不重复一遍检查逻辑）。
- README 9.1 的入口从 `扫码登录.pyw` 改成 `扫码登录.bat`，
  并说明了「为什么检测必须写在 bat 里、不能写在 pyw 里」。

## [0.4.0] - 2026-09-20

### 新增

- **图形界面版扫码登录：`扫码登录.pyw`，双击就能用。**
  不用命令行、不用 F12、不用装浏览器插件 —— 双击弹出一个窗口，
  里面直接显示二维码，手机扫一下、点确认，窗口里就变成
  「✅ 已登录：你的昵称」。
  - **没装 `qrcode` 也没关系**：界面上会出现「自动安装 qrcode」按钮，
    点一下自动装好并继续
  - 缺依赖、网络失败、二维码过期、扫完没确认……每种情况都有明确提示
  - 会弹个错误日志：图形界面一旦崩了，用户是**什么都看不到**的
    （窗口一闪就没），所以我们把崩溃信息落到 `扫码登录_错误日志.txt`
    并弹框告诉用户文件在哪
- **`tools/build_gui.ps1`：把它打包成单个 exe**（给机器上没装 Python 的人）。
  产物约 23 MB，放到项目根目录再双击即可。
- **`tools/check_gui.py`**：不靠截图，直接问 tkinter ——
  窗口建起来了没、二维码画出来没（画布上几百个方块、是不是正方形）、
  三种状态（等待扫码 / 已扫码 / 过期）能不能正确更新。
- `songboard/qrlogin.py`：扫码登录的逻辑抽出来，
  命令行版（`login_qrcode.py`）和图形界面版共用同一份，
  免得两边写两遍、改一处漏一处。

### 修复

- **打包脚本有两个坑，都是实测踩出来的**：
  - `$ErrorActionPreference = "Stop"` 会让 PowerShell 把
    `python -c "import 某模块"` 的 stderr（模块没装时的 traceback）
    当成致命错误，**脚本在"检查 PyInstaller"那一步就退出了**。
    改成 `Continue`，靠 `$LASTEXITCODE` 判断。
  - `--collect-subdirs` 这个参数**根本不存在**（PyInstaller 只有
    `--collect-submodules` / `--collect-all`），写错了会直接报参数错误。
- README 第九章的 9.1 换成图形界面版说明，项目结构和工具表都补了新文件。

### 说明

- `扫码登录.pyw` 双击即可运行（`.pyw` 不会弹黑色控制台窗口）；
  打包成 exe 只是为了让**没装 Python** 的人也能用。
- `.gitignore` 挡掉打包产物（`dist/`、`build/`、`*.spec`、exe 和错误日志）
  —— 20 多 MB 的二进制不适合进仓库。

## [0.3.0] - 2026-09-20

### 新增

- **扫码登录：`python login_qrcode.py`**。终端里直接画出二维码，用手机上的
  网易云音乐 App 扫一下、点确认，cookie 就自动写进配置并当场验证
  （打印出登录的昵称）。**不碰浏览器、不用 F12、不用复制粘贴。**
  需要 `qrcode` 这个纯 Python 小库（可选依赖，只有扫码时才用得到）。
- README 第九章补了对应小节，并把几种拿法按省事程度重新排了序。
- `songboard/cookies.py`：cookie 提取逻辑抽成共用模块，
  手动复制（`set_cookie.py`）和扫码登录走同一套；
  新增「从 `Set-Cookie` 响应头收凭据」—— 扫码成功时凭据在响应头里，不在 body。

### 说明

- **"自动读浏览器里的 cookie"这条路，现在走不通**（实测结论，写下来免得再试）：
  Chrome / Edge 127 之后给 cookie 换了 App-Bound Encryption，值以 `v20` 开头，
  密钥绑死浏览器自身身份。实测能拿到 DPAPI 密钥、能读 cookie 数据库
  （浏览器没在跑的时候），但**每一条 cookie 都是 v20，一条都解不出来**。
  硬要解就得往浏览器进程里注入代码 —— 本项目不做那种事。
  所以扫码是唯一真正的"全自动"。
- 编写时实测确认：unikey 接口可用、轮询返回 801（等待扫码）正常、
  终端二维码渲染正常、进度输出实时可见。
  ⚠️ **803（扫码成功那一步）需要真人拿手机扫，作者没法自测** ——
  代码里为此加了兜底：万一响应头里没有 `MUSIC_U`，会把收到的
  `Set-Cookie` 条目全部打印出来，便于定位。

## [0.2.8] - 2026-09-20

### 新增

- **README 新增第九章「快捷获取 Cookie」**：从「F12 → Network → Copy request
  headers」到一条命令写完并验证，四步走完；附三种拿法的对比表、
  检查有效性的一种方式、以及 cookie 过期时的表现。
- **`set_cookie.py` 重写**：
  - 从剪贴板读进来的内容，**任何形态都能认**：纯 cookie、带 `Cookie:` 前缀
    的一行、F12「Copy request headers」的整块、`Copy as cURL` 的整条命令
    （bash / cmd 都行）、甚至 JSON 里的 cookie 字段。
    实现上改成**按字段名正则搜值**，不再按 `;` 拆分配对 ——
    后者在真实粘贴内容上根本不可靠（cURL 里第一个字段名会变成
    `-H 'cookie: MUSIC_U`）。13 种形态 + 4 个反例都有测试。
  - **写完立刻验证**：调网易云账号接口，成功时打印
    `✅ cookie 有效，已登录：<昵称>`，失败时说明可能的原因 ——
    以前写完只能自己去猜有没有生效。
  - 删掉了早已废弃的歌单逻辑（`--playlist`、以及"没填歌单 ID 就不能测试"
    这类提示），现在只关心 cookie 能不能用。

## [0.2.7] - 2026-09-20

### 新增

- **控制台现在会显示「连的是哪个房间」**：`直播间 26259546《测试》`，
  状态行里还有房间名、分区、开播状态。
  这是排查"收不到弹幕"最需要的信息 —— 实测发现**房间号填错时，
  连接成功、认证成功、心跳正常、日志写着「弹幕连接已建立 ✅」，
  但一条弹幕都收不到**，只有把房间名亮出来才能一眼看出不对。
- `status()` 增加 `room` 字段（标题/主播/开播状态/人气/分区）。

### 修复

- **`diag_room.py` 用短号连接，会得出完全错误的结论**：它算出了真实房间号
  却没用它连弹幕服务器。实测 room 6（真实号 7734200）：
  - 用短号 6 连 → AUTH 成功、心跳正常，**0 条弹幕**
  - 用真实号 7734200 连 → 20 秒收到 3 条

  于是它对几十万人的大房间也会报"这个房间确实没有弹幕产生"，
  把人往完全错误的方向带。现在会先换号再连，并打印换号提示。
- `diag_room.py` 不再拿"历史弹幕为空"当结论：实测这个接口未登录时
  经常直接返回空（44 万人在线的房间也是 0 条），不足以判断房间没弹幕。
- 未开播的房间现在会明确提示「没开播就没有弹幕流，连上了也收不到
  （这是 B 站的机制，不是故障）」，而不是让人对着 0 条弹幕发呆。

## [0.2.6] - 2026-09-20

### 修复

- **cookie 过期会被误报成"搜不到这首歌"**。实测（2026-09）：不带有效 cookie
  调网易云搜索接口会返回 `code=50000005`（未登录）。以前这个码被忽略，
  搜到 0 条就报「网易云搜不到《稻香》」—— 主播会跑去检查歌名对不对，
  方向完全错了。现在会明确说
  「网易云登录态无效（code=50000005），cookie 可能已过期，请重新获取」。
  新增 `NeteaseAuthError` 专门区分"登录态问题"和"真搜不到"。
- **控制台的网易云状态标签从来没更新过**：HTML 里写死「未启用」，
  JS 里根本没有更新它的代码 —— 不管 cookie 填没填、有效没效，
  都显示「未启用」。
- **`driver.test()` 拿早已废弃的「歌单」来验 cookie**：只插播放队列之后
  歌单就不用了，于是没填歌单 ID 时它会报「缺少歌单 ID」，
  对现在的用法纯属误导。现在改成问网易云"我是谁"
  （`/w/nuser/account/get`），有效 cookie 能直接拿到昵称。
- **启动时不验证 cookie**：`account` 一直是空的，控制台分不清
  "有效"和"没验过"。现在启动时会自动验一次，日志里写明
  「网易云搜索：搜索可用，已登录：xxx」。

### 变更

- 控制台那张「网易云（播放队列）」卡片现在显示四种明确状态：
  `已登录：昵称` / `cookie 没能验证通过` / `没填 cookie，搜不了歌` /
  `未启用`；保存 cookie 后会当场把验证结果回显出来。
- `status()` 增加 `account` 字段（昵称 + uid）。
- README 补了一节「能不能不用 cookie」：实测结论是**不能** ——
  匿名老接口会限流（连打几十次就 `406`，等半分钟才恢复），
  直播场景不能依赖。

## [0.2.5] - 2026-09-20

### 修复

- **控制台顶部只显示相对路径 `/overlay`，没法直接填进 OBS**。现在显示的是
  拼好的完整地址（跟着你实际访问的地址走，换端口、换机器都对），
  旁边多了「复制」按钮，还有「预览」直接打开带背景的调试地址。
  （`navigator.clipboard` 拿不到时——非安全上下文——自动退回"选中 + 复制"。）
- `tools/check_overlay_dom.py` 加了对应检查：控制台的叠加层地址必须是完整
  URL 且指向当前访问的地址，预览链接也要是绝对地址。

## [0.2.4] - 2026-09-20

### 修复

- **`启动.bat` 现在会自检 Python 和依赖**。之前从 GitHub 下载 zip 解压、
  双击启动、选完房间号，直接抛
  `ModuleNotFoundError: No module named 'websockets'` 然后"已退出" ——
  屏幕上只有一段 traceback，看不出是"少装了一个包"。现在：
  - 缺依赖 → 自动 `pip install websockets`（装不上会给出手动命令）
  - 没装 Python → 明确提示去装 3.10+ 并勾选 *Add python.exe to PATH*
  - Python 版本太旧 → 直接报出当前版本
  - 没装 `cryptography` → 只提示一句（它只影响网易云搜索/查时长）
- `README` 的环境一节把 `websockets` 标成**必需**、`cryptography` 标成可选，
  并说明 `启动.bat` 会代劳。

## [0.2.3] - 2026-09-20

### 修复

- `proc_check.py` 的输出会被服务子进程污染：子进程的 `input()` 提示符
  （`点歌板> `）和 HELP 文本会**直接写到运行检查的那个终端**，
  把检查结果冲得看不见 —— 即使它的 stdout 已经接到管道了也没用
  （Windows 上 `input` 的提示符不走那个句柄；重定向到文件时看着正常，
  在终端里跑才露馅）。现在用 `CREATE_NO_WINDOW` 让子进程脱离当前控制台。

## [0.2.2] - 2026-09-20

### 修复

- **`启动.bat` 的中文被吃掉（严重）**：文件存的是 UTF-8 无 BOM，里面却写着
  `chcp 65001`。cmd.exe 读批处理文件是按**当前代码页逐字节解码**的，
  文件编码和运行时代码页一旦对不上，就会出现「整行被吃掉、后半截当成命令
  执行」的现象 —— 表现是菜单里的 `[2] 连直播间（需要填房间号）` 整行消失，
  还冒出一句
  `'需要填房间号）' is not recognized as an internal or external command`。
  两个 `.bat` 统一改成 **GBK 编码 + `chcp 936`**（中文 Windows 的原生代码页），
  读取和输出两端一致，不再在运行中切换代码页。
- `启动.bat` 里 `[1]` 的文案改为「演示模式（离线看效果；会自己造模拟弹幕，
  正式开播别用）」—— 原来的说法太含蓄，容易在正式开播时误选，
  选完还会把 `mode` 写成 `demo` 留在配置里。

### 新增

- `selftest.py` 新增「批处理文件编码」检查：所有 `.bat` 必须是
  GBK / CRLF / 无 BOM / 不含 `chcp 65001`，并确认菜单里的中文能读出来。
  故意改回 UTF-8 + `chcp 65001` 会被它拦下。

## [0.2.1] - 2026-09-20

### 修复

- **叠加层完全不刷新（严重）**：`web/overlay.html` 里 `let lastKey` 被写了两遍。
  同一段 `<script>` 内重复声明 `let` 属于早期语法错误，浏览器会把**整段脚本
  直接丢弃** —— 表现不是报错，而是叠加层安安静静地停在「等待点歌…」，
  服务端一切正常、日志一句不报。已删除重复声明。
  受影响的叠加层需要刷新浏览器源才会恢复。
- `proc_check.py` 里「叠加层可访问」拿响应**前 80 个字符**再断言 `点歌板`
  出现在其中，而它正好落在第 79 个字符附近 —— 文件换成 CRLF 换行后整体后移，
  检查当场假失败。改为取全文并断言结构性标记
  （`id="currentSong"` / `id="queue"`）。

### 新增

- `tools/check_overlay_dom.py`：用无头 Edge/Chrome 真跑一遍叠加层和控制台。
  以 `/api/state` 为标准答案，比对页面**实际渲染出来**的曲目，并用
  `?bg=1` 时 `<body>` 上的 `data-bg` 证明内联脚本真的执行了。
  上面那个"叠加层是死的"的坑，就是靠它复现并确认的。
- `selftest.py` 新增「网页内联 JS 语法」检查：装了 node 就 `node --check`
  真编译 `web/*.html` 里的内联脚本；没装 node 则退化成"同一块里重复声明"
  的粗筛。
- 命令行 `--version`，版本号取自 `songboard.__version__`。

### 变更

- `web/overlay.html` 的换行统一为 LF（与 `web/` 下其他文件一致）。

## [0.2.0] - 2026-09-20

### 新增

- **点歌门槛**：可以设成"送过礼物的人才能点歌"。
  - 四种判定方式：`min_total`（累计金额达标）、`any_paid`（送过任意付费礼物）、
    `per_send`（每点一首扣一次额度）、`guard_only`（仅舰长以上）。
  - 付费事件取自弹幕长连接：`SEND_GIFT` / `COMBO_SEND` / `GUARD_BUY` /
    `SUPER_CHAT_MESSAGE`，并且**只认 `paid=true` 的礼物** ——
    B 站会推银色瓜子的免费礼物（辣条），不过滤的话送个辣条就拿到资格。
  - 控制台新增「点歌门槛」卡片：开关、方式、金额、有效时长、只认付费礼物、
    舰长放行、醒目留言放行，保存立刻生效，另有「清空贡献记录」。
  - 命令行：`gift` / `gift on|off` / `gift min 2000` / `gift mode ...` /
    `gift window 600` / `gift list` / `gift reset`。
  - 被拦下的弹幕会写进日志和控制台「最近被拦」，并说明还差多少。
  - 新增 `demo_gift.py`：对着运行中的服务模拟送礼 / 上舰 / 醒目留言，
    调门槛不用求观众真刷礼物。
- `GiftLedger.qualified()`：只读判定，控制台刷新状态时不会把
  `allowed` / `blocked` 统计和拦截记录弄脏。
- 新增一系列自检用例（门槛判定、连击计费、时效、免费礼物、脏数据容错）。

### 修复

- **改门槛会清空所有人的送礼记录**：`reload_gift_gate()` 重建账本时没有搬走
  用户表，直播中途把门槛从 1000 调到 2000 就等于把观众送过的礼物清零。
- **`config.json` 带 BOM 会导致服务起不来**：记事本默认存成「UTF-8 带 BOM」，
  文件开头多一个 `\ufeff`，`json.loads` 直接抛 `Unexpected UTF-8 BOM`，
  报错完全看不出是 BOM 的问题。改为用 `utf-8-sig` 读取，保存时统一写不带 BOM
  的 UTF-8。JSON 真的写坏时也会给出人话报错（行号列号 + 常见原因），
  而不是甩一段 traceback。
- `selftest.py` 里等后台任务的地方存在竞态（断言可能跑在任务前面），
  统一成 `_drain_tasks()`，消掉偶发假失败。

## [0.1.0] - 2026-09-20

首个公开版本。

### 新增

- **弹幕长连接**：B 站弹幕服务器握手、鉴权（WBI 签名）、心跳、解压
  （brotli / zlib）、解析。关掉了 websockets 库的协议层保活，
  改用 B 站自己的心跳包 —— 上游不响应 websocket ping，用库自带的保活会每隔
  约 40 秒硬断一次。
- **弹幕指令**：`点歌` / `!点歌` / `#点歌` / `求歌`（前缀可配），
  以及取消点歌、查询、切歌；关键词列表留空即关闭对应指令。
- **点歌队列**：队列上限、每人限点、冷却、去重、取消、备选池、状态持久化。
- **播放队列桥**：注入 `AwooNcmCefBridge.dll`，通过 CEF DevTools 把歌插进
  网易云的**播放队列**（不是歌单）。一次只插一首 —— `addToNext` 是
  "插入到当前正在播放的曲目之后"，连插两首会得到反序。
- **网页叠加层**：给直播姬当浏览器源用，可调宽度、字号、主题、主色、显示条数。
- **网页控制台**：看队列、切歌、加歌、改房间号、改模式、看日志。
- **播放状态对齐**：读网易云真实播放状态，点歌板显示的就是实际在放的；
  可选接入外部媒体信息源拿精确进度用于自动切歌。
- **诊断与自检**：`selftest.py`（离线自检）、`proc_check.py`（进程级）、
  `check_integration.py`（集成）、`e2e_check.py`（端到端，有副作用）、
  `diag_room.py`（弹幕直连诊断）、`tools/` 下的注入与编译脚本。

### 说明

- 只支持 Windows：播放队列桥依赖进程注入和命名管道。
- 网易云的接口是非官方的，可能随官方改动失效。
- 桥需要每次重新注入（网易云一重启就失效）。

[0.4.2]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.4.1...v0.4.2
[0.4.1]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.4.0...v0.4.1
[0.4.0]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.8...v0.3.0
[0.2.8]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.7...v0.2.8
[0.2.7]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.6...v0.2.7
[0.2.6]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.5...v0.2.6
[0.2.5]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.4...v0.2.5
[0.2.4]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.3...v0.2.4
[0.2.3]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.2...v0.2.3
[0.2.2]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/QueKyoku/Que-bili-songboard/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/QueKyoku/Que-bili-songboard/releases/tag/v0.1.0
